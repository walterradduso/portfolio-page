import { ReactElement } from 'react';

import { Title } from '@/components/Title';

function Contact(): ReactElement {
  return (
    <section className="py-16">
      <Title subText="Get in Touch" text="Contact" />
    </section>
  );
}

export default Contact;
